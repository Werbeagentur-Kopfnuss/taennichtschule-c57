{% block be_block_table_link %}
    <div class="{{ class }} backend_rsce_block_table_link"{% if cssID %} id="{{ cssID }}"{% endif %}>

        <{{ headlineOrder }}>{{ headline }}</{{ headlineOrder }}>

    </div>
{% endblock %}