{% block be_block_page_teaser_standard %}
<div class="{{ class }} backend-rsce-block-page-teaser-standard"{% if cssID %} id="{{ cssID }}"{% endif %}>

    <{{ headlineorder }}>{{ pagename }}</{{ headlineorder }}>

    </div>
{% endblock %}